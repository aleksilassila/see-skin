import { Button } from "../../(components)/ui/button";

export function CreateSkinProfileFirst() {
  return (
    <div>
      <div>You don&apos;t have a SkinProfile set up.</div>
      <Button intent="primary">Create SkinProfile</Button>
    </div>
  );
}
