import { Button } from "../../(components)/ui/button";

export default function EditSkinProfilePage() {
  return (
    <div className="flex flex-col gap-2 items-start">
      <h1 className="text-xl font-medium">Delete SkinProfile</h1>
      <Button intent="primary">Delete SkinProfile</Button>
    </div>
  );
}
